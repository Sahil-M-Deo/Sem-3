#pragma once
#include "common.h"

// For each i, the first element right of arr[i] that is strictly greater than
// it, or -1 if there is none. Must run in O(n).
vector<int> nextGreaterElement(const vector<int> &arr);
